services:
  maps-service:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env
    restart: unless-stopped

  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - maps-service
